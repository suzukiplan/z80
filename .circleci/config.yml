version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: apt-get install -y clang-format
      - run:
          name: "Build and Test Z80 Emulator"
          command: "make"

workflows:
  test-workflow:
    jobs:
      - test
